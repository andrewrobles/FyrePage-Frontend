//const fs = require('fs');
//const files = fs.readdirSync(`${__dirname}/profiles`);

let data = {}
/*
files.forEach(f => {
    let fn = f.split('.')[0];
    data[fn] = require(f);
})
*/
export default data